{
  "name": "alexa-chatgpt-lambda",
  "version": "1.0.0",
  "description": "Alexa skill backed by ChatGPT via OpenAI Responses API",
  "main": "index.js",
  "scripts": {
    "build": "esbuild src/index.ts --bundle --platform=node --target=node20 --outfile=index.js --minify",
    "build:dev": "esbuild src/index.ts --bundle --platform=node --target=node20 --outfile=index.js",
    "watch": "npm run build:dev -- --watch"
  },
  "dependencies": {
    "ask-sdk": "^2.14.0",
    "googleapis": "^171.4.0",
    "openai": "^5.0.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "esbuild": "^0.24.0",
    "typescript": "^5.0.0"
  }
}
